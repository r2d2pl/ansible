- name: Changes packages :)
  vars:
    packages:
      - dstat
      - htop
      - perf
      - tiptop
      - sysstat
      - atop
      - lsscsi
      - sg3_utils
  dnf: 
    name: "{{ item }}"
    state: present
  with_items: "{{ packages }}"
