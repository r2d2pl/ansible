- name: Changes packages :)
  vars:
    packages:
      - dstat
      - htop
      - perf
      - tiptop
      - sysstat
      - atop
  dnf: 
    name: "{{ item }}"
    state: present
  with_items: "{{ packages }}"
